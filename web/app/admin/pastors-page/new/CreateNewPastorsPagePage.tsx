'use client';

import React from 'react';
import CreateEditPastorsPagePage from '../CreateEditPastorsPagePage';

export default function CreateNewPastorsPagePage () {
    return (
        <CreateEditPastorsPagePage
            pageTitle="Create New Pastor's Page Entry"
            submitFormAction="new"
        />
    );
}
